# Gemini Blog Pro - 开发者文档

## 🔧 开发环境设置

### 前置要求

- Node.js 16.x 或更高版本
- npm 或 yarn 包管理器
- Windows 操作系统（用于打包 Windows 应用）

### 安装依赖

```bash
cd d:\blogtest
npm install
```

---

## 🚀 开发模式

### 方式一：Web 开发模式

```bash
npm run dev
```

- 启动 Vite 开发服务器
- 访问 http://localhost:5173
- 支持热重载（HMR）

### 方式二：Electron 开发模式

```bash
npm run electron:dev
```

- 启动 Electron 桌面应用
- 自动打开开发者工具
- 支持热重载

> ⚠️ **注意**：运行 `electron:dev` 前需要先启动 Vite 开发服务器（`npm run dev`）

---

## 📦 构建生产版本

### 构建 Web 版本

```bash
npm run build
```

- 输出目录：`dist/`
- 可部署到任何静态网站托管服务

### 构建 Electron 桌面应用

```bash
npm run electron:build
```

- 输出目录：`release/`
- 生成文件：
  - `Gemini Blog Pro Setup 1.0.0.exe` - 安装程序（带安装向导）
  - `Gemini Blog Pro 1.0.0.exe` - 便携版（无需安装）
  - `win-unpacked/` - 未打包的应用文件

---

## 📁 项目结构

```
d:\blogtest/
├── electron/                  # Electron 主进程文件
│   ├── main.js               # 主进程入口
│   ├── preload.js            # 预加载脚本
│   └── icon.png              # 应用图标
├── src/                      # React 源代码
│   ├── components/           # React 组件
│   ├── services/             # 服务层（AI、导出、存储等）
│   ├── hooks/                # 自定义 Hooks
│   ├── App.jsx               # 主应用组件
│   └── main.jsx              # React 入口
├── dist/                     # Vite 构建输出（自动生成）
├── release/                  # Electron 打包输出（自动生成）
├── package.json              # 项目配置
├── vite.config.js            # Vite 配置
└── index.html                # HTML 入口
```

---

## ⚙️ 配置说明

### package.json 关键配置

```json
{
  "name": "gemini-blog-pro",
  "version": "1.0.0",
  "main": "electron/main.js",
  "scripts": {
    "electron:dev": "set NODE_ENV=development&& electron .",
    "electron:build": "npm run build && electron-builder"
  },
  "build": {
    "appId": "com.geminiblogpro.app",
    "productName": "Gemini Blog Pro",
    "win": {
      "target": ["nsis", "portable"],
      "icon": "electron/icon.png"
    }
  }
}
```

### Vite 配置（vite.config.js）

```javascript
export default defineConfig({
  plugins: [react()],
  base: './',              // Electron 需要相对路径
  build: {
    outDir: 'dist',
    emptyOutDir: true
  }
})
```

---

## 🔄 更新应用版本

### 1. 修改版本号

编辑 `package.json`：

```json
{
  "version": "1.1.0"
}
```

### 2. 重新构建

```bash
npm run electron:build
```

### 3. 分发新版本

将 `release/` 目录中的安装程序分发给用户。

---

## 🎨 自定义应用图标

### 准备图标文件

1. 创建 512x512 像素的 PNG 图标
2. 保存为 `electron/icon.png`

### 自动转换

electron-builder 会自动将 PNG 转换为 Windows 所需的 .ico 格式。

---

## 🐛 调试技巧

### 查看 Electron 日志

在 Electron 开发模式下，开发者工具会自动打开。

### 查看主进程日志

在 `electron/main.js` 中添加：

```javascript
console.log('Main process log');
```

日志会显示在启动 Electron 的终端中。

### 查看渲染进程日志

在 React 组件中使用：

```javascript
console.log('Renderer process log');
```

日志会显示在 Electron 的开发者工具控制台中。

---

## 📝 代码规范

### ESLint

项目已配置 ESLint，运行检查：

```bash
npm run lint
```

---

## 🚢 部署选项

### 选项 1: Electron 桌面应用（已完成）

- 适合：需要离线使用、桌面集成
- 分发方式：直接分享 .exe 文件

### 选项 2: Web 应用部署

#### Vercel（推荐）

```bash
npm install -g vercel
npm run build
vercel
```

#### Netlify

1. 运行 `npm run build`
2. 将 `dist/` 文件夹拖放到 netlify.com/drop

#### GitHub Pages

```bash
npm run build
# 将 dist/ 内容推送到 gh-pages 分支
```

---

## 🔐 安全注意事项

### API 密钥存储

- API 密钥存储在 localStorage 中
- 仅在本地存储，不会上传到服务器
- Electron 应用中的 localStorage 位于用户数据目录

### 内容安全策略（CSP）

已在 `index.html` 中配置 CSP：

```html
<meta http-equiv="Content-Security-Policy" 
      content="default-src 'self'; 
               script-src 'self' 'unsafe-inline' 'unsafe-eval'; 
               connect-src 'self' https://generativelanguage.googleapis.com https://api.pexels.com;" />
```

---

## 📚 依赖说明

### 核心依赖

- **React 19** - UI 框架
- **Vite 7** - 构建工具
- **Electron 39** - 桌面应用框架
- **@google/generative-ai** - Google Gemini API 客户端

### 功能依赖

- **mermaid** - 图表渲染
- **react-markdown** - Markdown 渲染
- **html2canvas** - 截图功能
- **jspdf** - PDF 导出
- **docx** - Word 导出
- **qrcode.react** - 二维码生成

---

## 🆘 常见开发问题

### Q: electron-builder 构建失败？

**A**: 确保：
1. 已安装所有依赖：`npm install`
2. 已先运行 `npm run build` 生成 dist 文件
3. 网络连接正常（需要下载 Electron 二进制文件）

### Q: Electron 窗口空白？

**A**: 检查：
1. `vite.config.js` 中的 `base: './'` 配置
2. `electron/main.js` 中的文件路径
3. 开发者工具中的错误信息

### Q: 如何减小安装包体积？

**A**: 
1. 移除不必要的依赖
2. 使用 `asar` 打包（electron-builder 默认启用）
3. 考虑使用 `electron-builder` 的压缩选项

---

## 📞 联系方式

如有技术问题，请提交 Issue 或联系开发团队。

---

**最后更新**：2026-01-10
